machine:
  node:
    version: 6.2.0

dependencies:
  pre:
    - npm install -g istanbul

general:
  artifacts:
    - ./coverage
  branches:
    only:
      - master

test:
  post:
    - bash <(curl -s https://codecov.io/bash)